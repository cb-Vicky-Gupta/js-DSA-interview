// Two Pointers and sliding Window

two pointers 
How to identify this pattern
The signal words to watch for in any problem:
What the problem saysWhat it means"sorted array" + "pair/triplet"Two pointers from both ends"palindrome check"Two pointers from both ends"remove duplicates in-place"Slow + fast pointer (same direction)"container with most water"Two pointers, shrink toward answer"three sum / four sum"Fix one element, two pointers on rest